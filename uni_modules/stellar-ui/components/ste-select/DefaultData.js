import utils from '../../utils/utils.js';

/**
 * 获取默认数据。
 * @param {"date"|"datetime"|"time"} mode - 模式，可以是 'date'、'datetime' 或 'time'。
 * @param {string} min - 最小值
 * @param {string} max - 最大值
 */
export function getDefaultData(mode = 'date', min = null, max = null) {
	return [];
}
